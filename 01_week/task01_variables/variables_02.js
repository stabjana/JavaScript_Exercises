const ageLimit = 18; // age limit doesn't change
console.log (ageLimit);
typeof ageLimit; //checking the type by this function

// Define a variable ageLimit that cannot be re-assigned and give it a value of 18.
// Console log the ageLimit value and check it's type