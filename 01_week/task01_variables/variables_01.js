let count = 0;      //count should be changeabble
console.log(count);
count++;                // incrementing the count
console.log(count);

// Define a variable called count with an original value of 0 and then increment it (add 1 to it) on the following line.