// Write the function sum such that it returns the sum of a and b.

function sum(a, b) {
    return a + b;
}

// const sum = (a, b) => a + b;

// sample usage - do not modify
console.log(sum(1, 3, 5)); // you should ignore the c value. The additional parameter is ignored automatically // if you put 3 Parameters, the solution for 2 Parameters shows NaN. To fix it you could use: Rest operator
console.log(sum(2, 5));

/*
function sum(a, b) {
    return console.log(a + b);
}
sum(1, 3, 5)
sum(2, 5)
*/