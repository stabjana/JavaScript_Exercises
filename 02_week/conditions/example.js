function isAdult(age) {
    if (age >= 18) {
        return true;
    }
    return false;
}

function isEligibleForVote(age) {
    return age >= 18;
}


// when to use if when to use switch
// switch: when you know the values that should be put in (example school roles: teacher...)

// use switch: when comparing one variable to many constant values
// use if: when evaluating different conditions or ranges (beispiel: age or name)

